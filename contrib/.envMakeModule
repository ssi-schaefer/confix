#!/bin/sh

# variables which have to be configured individually
export sourceDir=$HOME/cvs
export buildDir=$HOME/build
export packageDir=$HOME/Packages/wxp
# contains all existing modules"
export allModulesList="external utils toolscompat logtool entitycontainer hostcomm karstadt-business karstadt-hostcomm tsec"
# contains list of modules done all together by default
export defaultModuleList="external utils toolscompat logtool entitycontainer hostcomm karstadt-business karstadt-hostcomm" 

